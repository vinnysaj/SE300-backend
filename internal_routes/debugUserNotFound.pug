html
    head
        title User Not Found
        style.
            body {
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                background-color: #f4f4f4;
                display: flex;
                justify-content: center;
                align-items: center;
                height: 100vh;
                margin: 0;
                text-align: center;
            }
            .error-message {
                color: red;
                font-size: 20px;
                border: 1px solid red;
                padding: 15px;
                border-radius: 5px;
                background-color: white;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            }
        script.
            function redirectToDebugUser() {
                setTimeout(function() {
                    window.location.href = '/debugUser';
                }, 2000); 
            }
    body(onload="redirectToDebugUser()")
        .error-message
            | User #{user_id} not found
